import React from 'react'
import {
  Button
} from 'antd-mobile'

class Main extends React.PureComponent {
  render() {
    return(
      <Button type="primary">Main</Button>
    )
  }
}

export default Main
